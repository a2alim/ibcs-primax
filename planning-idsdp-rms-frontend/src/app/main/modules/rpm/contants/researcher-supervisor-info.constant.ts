import {environment} from '../../../../../environments/environment';

export const RESEARCHER_SUPERVISOR_INFO = environment.ibcs.rpmBackend + 'api/researcher-supervisor-Info/';
export const GET_BY_RESEARCHER_PROPOSAL_ID = 'get-by-researcher-proposal-id';
