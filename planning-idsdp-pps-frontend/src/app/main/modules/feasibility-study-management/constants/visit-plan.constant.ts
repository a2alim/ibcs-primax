import {environment} from '../../../../../environments/environment';

export const VISIT_PLAN = environment.ibcs.ppsFsBaseEndPoint + 'visit-plan/';
export const VISIT_PLAN_LIST_BY_FSP_MASTER_ID = 'getVisitPlanList';
