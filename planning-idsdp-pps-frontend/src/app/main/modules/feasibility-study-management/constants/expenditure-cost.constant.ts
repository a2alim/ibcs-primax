import {environment} from '../../../../../environments/environment';

export const EXPENDITURE_COST = environment.ibcs.ppsFsBaseEndPoint + 'expenditure-cost/';

export const GET_EXPENDITURE_COST_BY_FSP_MASTER_ID = 'getExpenditureCostList';
