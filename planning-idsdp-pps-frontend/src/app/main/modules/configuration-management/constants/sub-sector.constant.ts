import {environment} from '../../../../../environments/environment';

export const SUB_SECTOR = environment.ibcs.configurationApiEndPoint + 'subSector/';
export const GET_BY_SECTOR_ID = 'getBySectorId';
