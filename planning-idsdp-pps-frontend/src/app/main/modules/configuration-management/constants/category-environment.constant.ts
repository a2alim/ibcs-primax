import {environment} from '../../../../../environments/environment';

export const CATEGORY = environment.ibcs.configurationApiEndPoint + 'category/';
export const GET_ACTIVE_CATEGORY = 'getActiveCategory';
