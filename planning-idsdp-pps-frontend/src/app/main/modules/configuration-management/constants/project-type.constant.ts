import {environment} from '../../../../../environments/environment';

export const PROJECT_TYPE = environment.ibcs.configurationApiEndPoint + 'projectType/';
export const GET_ACTIVE_PROJECT_TYPE = 'activeProjectType';
