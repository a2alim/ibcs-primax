import {environment} from '../../../../../environments/environment';

export const USER_GROUP = environment.ibcs.configurationApiEndPoint + 'userGroup/';
export const GET_ACTIVE_USER_GROUP = 'activeUserGroup';
