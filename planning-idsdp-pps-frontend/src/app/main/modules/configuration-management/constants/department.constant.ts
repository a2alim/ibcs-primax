import {environment} from '../../../../../environments/environment';

export const DEPARTMENT = environment.ibcs.configurationApiEndPoint + 'department/';
export const GET_ACTIVE_DEPARTMENT = 'activeDepartment';
