import {environment} from '../../../../../environments/environment';

export const PARIPATRA_VERISON = environment.ibcs.configurationApiEndPoint + 'paripatraVersion/';

export const GET_ACTIVE_SINGLE_PARIPATRA_VERSION = PARIPATRA_VERISON +'activeSingleParipatra';
