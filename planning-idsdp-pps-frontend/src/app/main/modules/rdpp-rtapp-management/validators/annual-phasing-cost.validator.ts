import {AbstractControl, ValidationErrors} from '@angular/forms';

export class AnnualPhasingCostValidator {


    // static amountRangeValidator(control: AbstractControl): ValidationErrors | null {
    //
    //     if (control.value !== undefined && (isNaN(control.value) || control.value < control.value)) {
    //         return {amountRangeValidator: true};
    //     }
    //
    //     return {amountRangeValidator: false};
    // }
}
