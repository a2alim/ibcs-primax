spring:
  cloud:
    config:
      uri: http://202.161.191.131:9403/cloud-config-server
      enabled: true
      name: uaa-config
      profile: prod
      #profile: prod
management:
  endpoints:
    web:
      exposure:
        include: '*'
